using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;

namespace UnitTestProject1
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {
            Assert.AreEqual(Math.Round(Math.Tanh(Math.Pow(s, 2) + Math.Pow(s, -2) / Math.Pow(s, 2) - Math.Pow(s, -2)),
                Math.Round(ConsoleApp2.Program.Th(Math.Pow(s, 2) + Math.Pow(s, -2) / Math.Pow(s, 2) - Math.Pow(s, -2));
        }
    }
}
